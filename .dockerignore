# Git history — never needed inside a container
.git
.gitignore

# Node modules — installed fresh inside the container
**/node_modules

# Next.js build output — rebuilt inside the container
frontend/.next

# Local env files — secrets must come from docker-compose / runtime env
.env
.env.*
!.env.example

# Editor / OS noise
.vscode/
.idea/
**/.DS_Store
**/Thumbs.db

# Docs
*.md
